#include <stdio.h>
#include <stdlib.h>

int main(char argv[]){
    int i = 0, j, aux; 
    char aux[] = "Compiladores";
    
    if(i == 0){
        scanf("%d", aux); 

        for( ;aux < 10; aux++){
            printf("aux = %d\n", aux);
        }
    }
    return 0;
}